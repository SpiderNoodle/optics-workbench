# 版本履历 · 车载光学知识工作台

> **主站点（GitHub Pages）始终指向最新版本**：https://spidernoodle.github.io/optics-workbench/
> 历史版本 HTML 已归档在仓库 `versions/` 目录，也可在站内「版本履历」页（versions.html）浏览。
> 版本规则：**默认只进补丁号**（如 0.8.0→0.8.1）；minor/major 大版本变更需先与用户确认后再动。文件名带日期；旧版本保留不删。

---

## 工作台（Workbench）更新

### 2026-09-01 · v0.9.0 · Kindle主题可读性优化 + Office/WPS新主题 + 单位速查表格化 + 随机动画

- **Kindle 主题释义文字降亮**：针对 Kindle 主题下知识模块释义区文字过亮、难以辨识的问题，新增 `.define` / `.panel-desc` / `.spec-text` 等类的颜色覆盖样式，将释义文字统一调整为深灰 `#3a3a3a`，加粗文字为 `#1a1a1a`，大幅提升浅色背景下的可读性。
- **新增 Office/WPS 主题**：仿 Excel 办公风格主题，上班打开不显眼。配色采用 Excel 经典绿 `#217346` 品牌色 + 纯白卡片 + 浅灰细边框；圆角收敛为 2-4px（接近 Excel 单元格风格）；移除毛玻璃效果；滚动条改为 Excel 风格；表头使用深绿底白字；主题选择器新增 Office/WPS 选项，含专属色板图标和"Excel 办公风格"描述。
- **单位速查模块表格化**：将每个知识点的「数值·单位速查」块从列表/卡片形式改为**可滑动表格**形式。表格包含 5 列：符号 / 单位 / 含义 / 英文全称 / 范围说明；表头 sticky 固定，内容区 max-height 280px 可上下滑动，横向也可滚动；斑马纹 + hover 高亮；符号列使用等宽字体并品牌色高亮；运行时自动解析原有 .spec 块内容并转换为表格，切换知识点时自动重新转换。
- **随机模块换条动画**：为「换一条」按钮添加过渡动画效果。点击时内容先淡出（上移 + 模糊 + 缩放，0.3s），换条完成后淡入（下移 + 清晰 + 缩放，0.4s）；按钮图标同步旋转 360°；动画受「开启动效」开关约束，关闭动效时无动画直接切换。
- **版本号**：v0.8.9 → v0.9.0；sw.js 缓存版本同步至 `optics-workbench-v0.9.0`。
- **构建脚本**：新增 `build_v090.py`，包含上述四项修改的完整自动化逻辑，支持诊断输出。

### 2026-08-21 · v0.8.4 · SOP 补齐 + 首页/分类/释义折叠优化
